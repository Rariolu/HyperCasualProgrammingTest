using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum SCENE
{
    TITLE = 0,
    LOADING = 1,
    GAME = 2,
    END = 3
}

public enum TAG
{
    PLAYER,
    WALL
}